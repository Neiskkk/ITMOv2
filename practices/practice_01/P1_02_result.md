```
Summary:
- В PR добавлен метод ReviewService.review и эндпоинт POST /api/reviews, которые формируют prompt из входного diff и вызывают llm.generate(prompt), возвращая результат как {"comment": answer}.
Подтвержденные риски (QA-1)
1. Нарушение SEC-1: передача diff во внешний LLM без редактирования секретов
- file: app/review_service.py, line: 19-22 (по TRAINING_PR.diff блок @1,10→1,23; конкретно добавленные строки)
- evidence:
- line 20: prompt = f"Review this pull request and find problems:\n{diff}"
- line 21: answer = self.llm.generate(prompt)
- risk: Diff напрямую включается в prompt без упоминания маскирования секретов. Это создает риск утечки секретов (SEC-1).
- Проверка: Подать diff с тестовыми секретами (например, TOKEN=abcd1234) и убедиться, что в prompt подставляется REDACTED вместо значения. Если нет — нарушение подтверждается.
2. Нарушение OUT-1: неверный формат ответа
- file: app/review_service.py, line: 22 (возвращаемая структура)
- evidence:
- line 22: return {"comment": answer}
- risk: Формат результата не соответствует OUT-1: отсутствуют поля summary, risks (<=3 с file,line,evidence,risk), checks. Клиент не сможет полагаться на контракт.
- Проверка: Контрактная проверка JSON-схемой — ожидать наличие summary, risks, checks. Текущий код не соответствует.
3. Потенциальное нарушение API-1: отсутствие обработки размера diff > 20 000 символов
- file: app/api.py, line: 35-37 (новый эндпоинт)
- evidence:
- line 35: @app.post("/api/reviews")
- line 37: return review_service.review(payload"diff")
- risk: Вставка diff без явной проверки размера может привести к несоответствию API-1. В текущем diff нет логики возврата HTTP 413 для больших diff.
- Проверка: Отправить diff длиной > 20 000 символов и ожидать HTTP 413. Если сервис принимает и обрабатывает, нарушение подтверждено.
Checks (что стоит запустить)
- Проверка SEC-1: POST /api/reviews с payload.diff, содержащим тестовый токен/секрет; трассировка формируемого prompt или мок LLM для проверки, что секреты заменены на REDACTED.
- Проверка OUT-1: Контрактные тесты на структуру ответа: нужно наличие summary, risks (<=3, поля file,line,evidence,risk), checks. Текущая реализация вернет {"comment": ...}, что должно упасть.
- Проверка API-1: Нагрузочный тест с diff > 20 000 символов и ожидание HTTP 413. Зафиксировать фактический статус.
Замечания по формату и границам
- Границы соблюдены: не изменял код, не одобрял PR, использовал только разрешенные источники и описал воспроизводимые проверки.
- Каждое утверждение снабжено file:line и цитатой строки diff либо правилом CASE.md.
Следующие шаги
- Документы context.md, problem.md и prompts.md заполнены и соответствуют требуемому формату. Context Pack включает только релевантные правила (SEC-1, API-1, OUT-1, SCOPE-1, QA-1).
- Если нужно, выполню P1-02 запуск, используя Master Prompt v1, и заполню секцию сравнения двух запусков на основе фактических результатов.
```